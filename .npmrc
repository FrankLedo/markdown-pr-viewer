# Supply-chain hardening: never auto-run package lifecycle scripts
# (preinstall/install/postinstall/prepare) on `npm install`. Lifecycle scripts
# are the primary npm malware vector. Tools that need a post-install step must be
# run explicitly (e.g. `npm rebuild esbuild`, `npx playwright install`, `npx husky`).
ignore-scripts=true
